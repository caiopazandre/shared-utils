# Databricks entry point (exemplo)
from data_recovery.services import RollbackService

# service = build_rollback_service_from_databricks()
# table_name = dbutils.widgets.get("table_name")
# dbutils.notebook.exit(str(service.execute(table_name)))
